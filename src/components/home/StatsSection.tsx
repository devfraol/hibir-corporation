import { useQuery } from "@tanstack/react-query";
import ScrollReveal from "@/components/ScrollReveal";
import { getStatistics } from "@/services/companyService";
export default function StatsSection(){const {data=[],isLoading}=useQuery({queryKey:["company","statistics"],queryFn:getStatistics});return <section id="at-a-glance" aria-labelledby="capacity" className="relative section-padding overflow-hidden scroll-mt-24"><div className="container-custom relative"><ScrollReveal className="max-w-3xl mb-14"><span className="label-eyebrow">Company at a Glance</span><h2 id="capacity" className="section-title mt-4">The scale behind every kilometre we deliver</h2></ScrollReveal>{isLoading?<div className="h-40 animate-pulse rounded-2xl bg-muted"/>:data.length?<div className="grid grid-cols-2 gap-px overflow-hidden rounded-2xl border border-border bg-border md:grid-cols-3">{data.map(s=><div key={s.id} className="bg-background p-7"><div className="font-display text-2xl font-bold text-gradient-gold">{s.displayValue}</div><p className="mt-2 text-sm text-muted-foreground">{s.label}</p></div>)}</div>:null}</div></section>}
